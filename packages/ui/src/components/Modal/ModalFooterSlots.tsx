import { FC, PropsWithChildren } from "react";

export const ModalFooterBefore: FC<PropsWithChildren> = ({ children }) => (
  <>{children}</>
);

export const ModalFooterAfter: FC<PropsWithChildren> = ({ children }) => (
  <>{children}</>
);

export const ModalFooterBtnBack: FC<PropsWithChildren> = ({ children }) => (
  <>{children}</>
);

export const ModalFooterBtnCancel: FC<PropsWithChildren> = ({ children }) => (
  <>{children}</>
);

export const ModalFooterBtnConfirm: FC<PropsWithChildren> = ({ children }) => (
  <>{children}</>
);
